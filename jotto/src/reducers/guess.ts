// import { actionTypes } from '../actions'

const guessReducer = (state: string = '', action: ActionTypes): string => {
  switch (action.type) {
    default:
      return state
  }
}

export default guessReducer
